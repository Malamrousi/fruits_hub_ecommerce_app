import 'package:flutter/material.dart';

class CustomCartDivider extends StatelessWidget {
  const CustomCartDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
        color: Color(0xFFEBF9F1),
        height: 22,
      );
  }
}